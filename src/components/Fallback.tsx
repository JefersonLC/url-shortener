import { Pulsar } from "@uiball/loaders";

export default function Fallback() {
  return (
    <section className="min-h-screen bg-slate-800 flex justify-center items-center">
      <Pulsar size={100} color="#F59E0B" />
    </section>
  );
}
